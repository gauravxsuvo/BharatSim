# Lightweight backend runtime — runs the full API on SQLite with NO Docker,
# NO PostGIS, and NO heavy ML wheels (torch/xgboost/lightgbm/geopandas).
# Verified on Python 3.14.
#
#   python -m venv venv && ./venv/Scripts/activate   # (Windows)
#   pip install -r requirements-lite.txt
#   python -m app.seed && uvicorn app.main:app --reload
#
# For the full ML + PostgreSQL/PostGIS stack instead, use `pip install -e .`.

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.20.0
geoalchemy2>=0.15.0
pydantic-settings>=2.0.0
python-multipart>=0.0.9
httpx>=0.27.0
pandas>=2.2.0
shapely>=2.0.0

# Tests (optional)
pytest>=8.0.0
pytest-asyncio>=0.23.0
